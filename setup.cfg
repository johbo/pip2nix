[tool:pytest]
norecursedirs = .* _bootstrap_env *.egg result* tmp
testpaths = tests/unit
addopts = --strict-markers
markers =
    nix: needs the nix tools on PATH
    network: resolves against a real package index
